#include <stdio.h>

int main(void) 
{
  int s=0, i, n;
  printf("\n enter the number");
  scanf("%d", &n);
  while(n!=0)
  {
    i=n%10;
    s=s+i;
    n=n/10;
  }
  printf("\n sum of digits:%d", s);
}
